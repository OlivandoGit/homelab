---
- name: docker_volumes directory
  ansible.builtin.file:
    path: /mnt/docker_volumes
    state: directory

- name: Mount docker_volumes
  ansible.posix.mount:
      src: truenas.home.olivando.me:/mnt/fast-storage/docker_volumes
      path: /mnt/docker_volumes
      fstype: nfs
      state: mounted
